machine:
  node:
    version: 6.5.0

test:
  override:
    - set -o pipefail && npm -s run test:xunit > ${CIRCLE_TEST_REPORTS}/tape.xml
    - npm run lint
deployment:
  releases:
    branch: master
    commands:
      - npm publish