machine:
  node:
    version: 6.5.0

test:
  override:
    - set -o pipefail && npm test > ${CIRCLE_TEST_REPORTS}/tape.xml
    - npm run lint
    - npm -s run coverage
deployment:
  releases:
    branch: master
    commands:
      - npm publish